# Built binaries
/V2bX
/v2bx
*.exe

# Geo routing data — large, downloaded at deploy time, must NOT be committed
*.dat
*.db
example/geoip.dat
example/geoip.db
example/geosite.dat
example/geosite.db

# Real runtime config / secrets (keep only example/*.json templates)
/config.json
/etc/
*.log

# Test-generated certs / ACME state (regenerated by `go test`, never committed)
node/1.key
node/1.pem
node/cert/
test_data/

# Go / editor
/dist/
vendor/
.idea/
.vscode/
.DS_Store
